<%- include ('partials/header.ejs') %>
    <body>
        <h1 class = "title">Add Admin</h1>
        <main>
         <form action="/api/updateAdmins">
            <input type="text" placeholder = "Type Username" name ="username" required/>
            <input type="text" placeholder = "Type password" name ="password" required/>
            <br><br>
            <button class="btn btn-warning">Update</button>
        </form>
        
         <!--Checks if product already exists throws err-->
        <% if (typeof loginError != "undefined" && loginError){ %>
        <h3 class="error">Admin Username Already Exists!</h3>
        <% }%>
        </main>
<%- include ('partials/footer.ejs') %>